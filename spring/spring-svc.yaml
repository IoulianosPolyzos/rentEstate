apiVersion: v1
kind: Service
metadata:
  labels:
    app: spring
  name: spring
spec:
  ports:
    - port: 8081
      protocol: TCP
      targetPort: 8081
      name: spring
  selector:
    app: spring
  type: ClusterIP
